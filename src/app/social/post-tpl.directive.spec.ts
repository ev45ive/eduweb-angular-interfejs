import { PostTplDirective } from './post-tpl.directive';

describe('PostTplDirective', () => {
  it('should create an instance', () => {
    const directive = new PostTplDirective();
    expect(directive).toBeTruthy();
  });
});
