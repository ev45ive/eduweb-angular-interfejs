import { PostOutletDirective } from './post-outlet.directive';

describe('PostOutletDirective', () => {
  it('should create an instance', () => {
    const directive = new PostOutletDirective();
    expect(directive).toBeTruthy();
  });
});
